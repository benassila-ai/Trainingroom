import styled from "@emotion/styled";
import {
  Grid2 as MatGrid2,
  TableContainer as MatTableContainer,
} from "@mui/material";

export const Grid2 = styled(MatGrid2)`
  display: 'flex';
  justify-content: 'space-between';
`;

export const TableContainer = styled(MatTableContainer)`
  min-width: 650px;
  margin-top: 5px;
`;
